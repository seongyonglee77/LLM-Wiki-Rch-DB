# llm-wiki Instructions for Claude-Compatible Agents

Use `AGENTS.md` as the authoritative local operating contract. This file exists only as an adapter for agents that preferentially read `CLAUDE.md`.

The active root is this folder. Treat `refs.bib` and project `references.bib` files as generated output. Never silently overwrite locked cards or unrelated bibliography entries.

